import { BeterHighlightDirective } from './beter-highlight.directive';

describe('BeterHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BeterHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
